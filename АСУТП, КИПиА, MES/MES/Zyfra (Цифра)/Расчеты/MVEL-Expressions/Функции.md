# Математические функции из класса `java.lang.Math` (Java SE 21)
**🔗 Источник:** [Официальная документация Java Math](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Math.html)

## Основные математические операции

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **Абсолютное значение** | `Math.abs(a)` | Возвращает абсолютное значение числа | `Math.abs(-5) = 5` |
| **Округление** | `Math.round(a)` | Округление до ближайшего целого | `Math.round(3.7) = 4` |
| **Округление вверх** | `Math.ceil(a)` | Округление до большего целого | `Math.ceil(3.2) = 4` |
| **Округление вниз** | `Math.floor(a)` | Округление до меньшего целого | `Math.floor(3.9) = 3` |
| **Максимум** | `Math.max(a, b)` | Возвращает большее из двух чисел | `Math.max(5, 3) = 5` |
| **Минимум** | `Math.min(a, b)` | Возвращает меньшее из двух чисел | `Math.min(5, 3) = 3` |
| **Случайное число** | `Math.random()` | Случайное число от 0.0 до 1.0 | `Math.random() = 0.547...` |
| **Знак числа** | `Math.signum(a)` | Возвращает знак числа (-1, 0, 1) | `Math.signum(-5) = -1` |

## Степенные и логарифмические функции

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **Возведение в степень** | `Math.pow(a, b)` | Число a в степени b | `Math.pow(2, 3) = 8` |
| **Квадратный корень** | `Math.sqrt(a)` | Квадратный корень из числа | `Math.sqrt(16) = 4` |
| **Кубический корень** | `Math.cbrt(a)` | Кубический корень из числа | `Math.cbrt(27) = 3` |
| **Экспонента** | `Math.exp(a)` | e в степени a | `Math.exp(1) ≈ 2.718` |
| **Натуральный логарифм** | `Math.log(a)` | Натуральный логарифм (основание e) | `Math.log(10) ≈ 2.302` |
| **Логарифм по основанию 10** | `Math.log10(a)` | Десятичный логарифм | `Math.log10(100) = 2` |
| **Логарифм (1 + x)** | `Math.log1p(x)` | `ln(1 + x)` с высокой точностью | `Math.log1p(1) ≈ 0.693` |

## Тригонометрические функции

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **Синус** | `Math.sin(a)` | Синус угла в радианах | `Math.sin(π/2) = 1` |
| **Косинус** | `Math.cos(a)` | Косинус угла в радианах | `Math.cos(0) = 1` |
| **Тангенс** | `Math.tan(a)` | Тангенс угла в радианах | `Math.tan(π/4) ≈ 1` |
| **Арксинус** | `Math.asin(a)` | Арксинус (возвращает радианы) | `Math.asin(1) = π/2` |
| **Арккосинус** | `Math.acos(a)` | Арккосинус (возвращает радианы) | `Math.acos(1) = 0` |
| **Арктангенс** | `Math.atan(a)` | Арктангенс (возвращает радианы) | `Math.atan(1) = π/4` |
| **Арктангенс точки** | `Math.atan2(y, x)` | Арктангенс координат y/x | `Math.atan2(1, 1) = π/4` |

**💡 Примечание:** Все тригонометрические функции работают с углами в радианах. Для преобразования используйте `Math.toRadians()` и `Math.toDegrees()`.

## Гиперболические функции

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **Гиперболический синус** | `Math.sinh(a)` | Гиперболический синус | `Math.sinh(0) = 0` |
| **Гиперболический косинус** | `Math.cosh(a)` | Гиперболический косинус | `Math.cosh(0) = 1` |
| **Гиперболический тангенс** | `Math.tanh(a)` | Гиперболический тангенс | `Math.tanh(0) = 0` |

## Специальные математические функции

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **Остаток от деления** | `Math.IEEEremainder(x, y)` | Остаток по стандарту IEEE 754 | `Math.IEEEremainder(10, 3) = 1` |
| **Гипотенуза** | `Math.hypot(x, y)` | `sqrt(x² + y²)` без переполнения | `Math.hypot(3, 4) = 5` |
| **Разность квадратов** | `Math.multiplyHigh(x, y)` | Старшие 64 бита произведения | |
| **Точное умножение** | `Math.multiplyFull(x, y)` | Полное произведение двух int | |
| **Умножение с накоплением** | `Math.fma(a, b, c)` | `a * b + c` с одной операцией | `Math.fma(2, 3, 1) = 7` |

## Функции округления и точности

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **Округление до целого** | `Math.rint(a)` | Округление до ближайшего целого | `Math.rint(3.5) = 4` |
| **Следующее число** | `Math.nextAfter(a, b)` | Следующее число после a в направлении b | |
| **Следующее вверх** | `Math.nextUp(a)` | Следующее число больше a | |
| **Следующее вниз** | `Math.nextDown(a)` | Следующее число меньше a | |
| **Масштабирование** | `Math.scalb(a, b)` | `a × 2^b` | `Math.scalb(3, 2) = 12` |
| **Получить экспоненту** | `Math.getExponent(a)` | Экспонента числа в представлении float/double | |

## Константы

| Константа | Синтаксис | Значение | Описание |
|-----------|-----------|----------|----------|
| **Число π** | `Math.PI` | ≈ 3.141592653589793 | Отношение длины окружности к диаметру |
| **Число e** | `Math.E` | ≈ 2.718281828459045 | Основание натурального логарифма |
| **Бесконечность** | `Math.Infinity` | Infinity | Положительная бесконечность |
| **Не число** | `Math.NaN` | NaN | "Not a Number" - не число |

## Функции для работы с углами

| Функция | Синтаксис | Описание | Пример |
|---------|-----------|----------|--------|
| **В радианы** | `Math.toRadians(deg)` | Преобразование градусов в радианы | `Math.toRadians(180) = π` |
| **В градусы** | `Math.toDegrees(rad)` | Преобразование радианов в градусы | `Math.toDegrees(π) = 180` |

## Усовершенствованные математические функции

| Функция | Синтаксис | Описание |
|---------|-----------|----------|
| **Точное сложение** | `Math.addExact(a, b)` | Сложение с проверкой переполнения |
| **Точное вычитание** | `Math.subtractExact(a, b)` | Вычитание с проверкой переполнения |
| **Точное умножение** | `Math.multiplyExact(a, b)` | Умножение с проверкой переполнения |
| **Точное инкремент** | `Math.incrementExact(a)` | a + 1 с проверкой переполнения |
| **Точное декремент** | `Math.decrementExact(a)` | a - 1 с проверкой переполнения |
| **Точное отрицание** | `Math.negateExact(a)` | -a с проверкой переполнения |
| **Точное преобразование** | `Math.toIntExact(a)` | long в int с проверкой переполнения |
